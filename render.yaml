# Render.com blueprint: push to GitHub, then "New + > Blueprint" and point it at this repo.
services:
  - type: web
    name: who-is-it
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
