# Copy to ".env" on your server and set your image. The username/repo are whatever you name the
# GitHub repo you push to (see the CI in .github/workflows/deploy.yml).
WHO_IMAGE=ghcr.io/your-github-username/who-is-it:latest
